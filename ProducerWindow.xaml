<Window x:Class="aehc.ProducerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:aehc"
        mc:Ignorable="d"
        Title="Consume Events"
        MinHeight="450"
        MinWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="73*"/>
            <ColumnDefinition Width="327*"/>
        </Grid.ColumnDefinitions>
        <TextBox Name="fullyQualifiedNameSpaceBox" Grid.ColumnSpan="2" Margin="18,15,199,394"></TextBox>
        <TextBox
            Name="eventBodyBox"
            Grid.ColumnSpan="2" 
            Margin="18,58,199,120"
            ScrollViewer.VerticalScrollBarVisibility="Visible">
        </TextBox>
        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="okButton" Click="Click_OkButton" IsDefault="True">OK</Button>
            <Button Name="cancelButton" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>

</Window>
